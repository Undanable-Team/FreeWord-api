'use strict';

/**
 * state-authority controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::state-authority.state-authority');
